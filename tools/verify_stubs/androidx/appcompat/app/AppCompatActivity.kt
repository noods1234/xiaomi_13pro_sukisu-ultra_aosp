package androidx.appcompat.app
// SHIM (verification only): real signature subset. androidx is Google-Maven-only and unreachable
// here, so these classes are verified at a WEAKER tier than the android-all classes.
open class AppCompatActivity : android.app.Activity(), androidx.lifecycle.LifecycleOwner

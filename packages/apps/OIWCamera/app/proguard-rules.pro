# No rules needed yet — release build currently ships with isMinifyEnabled = false
# (docs/BUILD_SYSTEM.md #5). Add Gson TypeToken/reflection keep rules here before ever
# enabling minification, since CaptureProfile/ClipMetadata rely on reflective (de)serialization.

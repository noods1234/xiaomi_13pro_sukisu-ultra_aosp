org.gradle.jvmargs=-Xmx2048m
android.useAndroidX=true
kotlin.code.style=official

# oiw.release.keystore / oiw.release.storePassword / oiw.release.keyAlias / oiw.release.keyPassword
# are expected in a local, gitignored gradle.properties override (see docs/BUILD_SYSTEM.md #6) —
# never commit real signing credentials here.

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "OIW-ROM Lens Profile (metadata defaults only)",
  "description": "Metadata defaults only, not optical-correction coefficients — those require Tier 5/6 HAL access, which is blocked (docs/CAMERA_PIPELINE.md #2, assumptions.md A-3).",
  "type": "object",
  "required": ["id", "lensName"],
  "properties": {
    "id": { "type": "string" },
    "lensName": { "type": "string" },
    "focalLengthMm": { "type": "number" },
    "aperture": { "type": "string" },
    "adapter": { "type": "string" },
    "speedBoosterFactor": { "type": "number" },
    "anamorphicSqueeze": { "type": "number" },
    "distortionProfileNotes": { "type": "string" },
    "calibrationNotes": { "type": "string" }
  }
}
